# Base URL of the GovPath FastAPI backend.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Set to 1 to use hand-written mock data instead of hitting the backend.
# Useful for developing the UI before the API is live.
NEXT_PUBLIC_USE_MOCK=0
