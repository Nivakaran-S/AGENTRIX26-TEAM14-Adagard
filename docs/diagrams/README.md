Owner: Person C. Build these for the Final Report (T+9 to T+11):
- er_diagram        — sessions, verifications, circulars/chunks, forms
- architecture      — Flutter app + Admin web -> FastAPI -> LangGraph agents -> ChromaDB + Gemini
- use_case          — Citizen (chat, get plan) ; Officer (review, approve) ; System (scrape/vectorize)
Tool tip: draw.io / Mermaid / Figma. Export PNG into this folder.

➡️ Ready-to-use, code-accurate diagrams (Mermaid + PlantUML) live in **`DIAGRAMS.md`**:
architecture/multi-agent flow, as-built + production-target ER, and the use-case diagram —
each traced to the source files. Rendered PNGs are exported here as `govpath-*.png`.
